#include "vector3.h"

math::Vector3::Vector3() {
    Initialize(0.0f, 0.0f, 0.0f);
}

math::Vector3::Vector3(float x, float y, float z) {
    Initialize(x, y, z);
}

void math::Vector3::Initialize(float x, float y, float z) {

}